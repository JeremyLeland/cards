3/11:

Still would like to try rasterizing the SVG at the precise width/height for the current canvas size
  - Scaling is working ok, but seems like there are bluriness or aliases in either direction

DONE Put all the old canvas functionality back in
  - Well, except for update
  - testBoard doesn't use that

_ Use whole numbers for positions, make card smaller

3/10:

DONE: Scaling to fit
  - Set Card.Width to 1 and padding to a fraction thereof
  - Find the proper scale to fit 7 cards width-wise

Tweaking SVG loading and scaling
  - Fullscreen needs a bigger SVG
  - Scaling with createImage resizeWidth is making things blurry
    - Seems like if I load by making an Image() and setting src, it scales up rastered image
    - ChatGPT is suggesting the fetch/blob thing that I couldn't get to work before
    - Maybe worth another shot now
      - Kept getting DOM errors
      - This way works fine and lets me see the image size without shenanigans
      - Just keep it

Scale based on height as well as width
  - We want enough space in the tableau for a lot of cards (not sure if there's a limit?)
  - If we go full width on widescreen, no room for tableau
  - Come up with a minimum height and limit that as needed
    - Should also center in that case

Simplify my position numbers
  - Making all the card positions be whole numbers
  - Make Card.Width be less than 1?
  - This should put padding on both sides of table

pointerEvent movementX/movementY seem unreliable for touch events
  - Firefox has them going wrong direction? Or smaller
  - Chrome has them smaller
  - Maybe I should save last position and calculate these myself?
    - Less elegent, was hoping I could just use movementX...

It's really seeming like I had the right idea doing all of this myself in my custom canvas
  - May end up pretty much doing that again
  - With multiple places to keep track of scale and scroll (for drawing and input)
    - probably best to do it all in one file


3/9:

Put in save/load state by bringing in GameState.js

Made a simpler version of my Canvas class in GameCanvas.js
  - Seeing if I can do without the updating and input
    - More of this should be doable with stock js
    - However, the tie between canvas scale/scroll and input positions makes the larger class more tempting...

Put scroll and scale in, in preparation for resizing based on browser size
  - Still using my original positions
  - Seems like these could be changed to multiples of 1 for simplicity, maybe at later point
  - If I do this, I think I need to set Card.Width to 1 (so Card.Height gives me the relative ratio)
    - I can probably continue to use Card.Width and Card.Height to plan positions
      - I should make the Offsets relative to Card.Width/Height

Can I center the canvas separately, so the input fixing doesn't have to know about that?

3/7:

Save/load state
  - Should be quick, and makes easier to test fixes if board state isn't always reseting

3/6:

Demo: lots of flipping cards
  - Scale destination when drawing cards, change this amount by sin( time )
  - Should have a bunch of "flipping" cards.
  - Can also use this animation to test framerate with various drawing methods

Resize so board always fits in window
  - Divide canvas width by 7? (need to turn Position arrays into functions, then)
  - Or use constant locations (where card size is 1?) and scale canvas to fit?

3/5:
Changing active to use startIndex instead of card (to support multiple cards from tableau)
  - Fix input
  - Fix drawing

DONE: Can move stacks of cards between tableaus.

DONE: Start implementing rules to validate moves
  - Probably need a way of knowing the type of destination stack
    - Would it be better to store type and index instead of the actual stack?
    - indexOf get there without changing data structure, but seems awkward
      - ended up leaving it this way for now

I think I have the logic all in place!

Now I just need a way to win and start over.
Also, save/load progress when reloading page (should make testing much nicer!)

Can fix bluriness by drawing SVG directly and not pre-rasterizing
  - However, performance is noticeably terrible on my laptop
  - The pre-rasterizing seemed like a good idea,
  - It would be nice if I could figure out how to not make it blurry
    - Getting rid of my pre-scaling during rasterization seems to help
    - Now some scaling is happening as part of every drawImage, I would think
      - But it looks sharper
      - And it's snappy on my laptop
      - So I guess it's fine!


3/4:

Things still seem kind of blurry with the SVG
  - Try not to go too much down a rabbit hole on this
  - You can go back to canvas drawn cards at some point to troubleshoot
  - But for now, please just focus on making a playable game!

Try to do tonight:
  + DONE: Pull cards from waste
  + DONE: Click deck to move cards to waste
    - DONE: Still need to pull cards from waste back into stock when stock is empty

Visually, should we have offsets for deck and foundation to give a sense of how many cards are there?
  - Eh, no one else seems to

NEXT: Support moving multiple cards from tableaus
  - Make active.card be an array (always? just in this case?)
  - Draw multiple cards with appropriate offsets if active.card is an array
THEN: Make sure move is valid before letting a card move

3/3:

Collision detection
  - For initial click, check against the waste and tableaus (similar to draw loop)
  - As we move, check against foundations and tableaus for valid landing zones
    - Draw to indicate this somehow, maybe translucent version of card snapped into position?
  - When we release, either snap to new position (and move from old tableau)
    - or return to original position

Drawing
  - Draw translucent version of active card wherever current move would end
    - If no valid move yet, draw at old position
    - If valid move, draw at new position
  - Draw empty card outline (manually) at foundation positions
  - Don't track face up/down for individual cards, we know it when drawing based on position
    - Don't reveal new tableau card while active move in progress
    - Add cheat mode to reveal all tableau cards for debugging?

Tracked face up/down (turns out I do need this, if we're going to stack cards in tableau)
Supported placing cards in foundation

Still need translucent old/new markers

Support pulling cards from waste
Click on deck to put more cards in waste

Need to check rules for if move is valid before allowing it
  - Probably easier to support all the other input first
    - Make sure I can drag everything around everywhere before I start limiting it with rules

3/2:

Made an example layout for Klondike style solitaire
  - Need to draw the empty Foundations spots
  - Need to support face-down cards

Will need to limit input
  - Letting go of an active drag should reset the card to where it started
    - Keep track of oldX and oldY as part of active? active = { card: card, oldX: int, oldY: int }?
  - Should only be able to click on face-up cards
  - Can we interact with foundations after we've dropped cards there?
  - Clicking on stock should reveal card(s), but not make one active
    - Kind of a waste to check location against 50 cards in the same spot

Might make more sense to store cards (without location) in lists of deck, foundation, tableau, etc
  - Only active will have a location?
  - Will need to know what the top card is over various foundations and tableaus in order to know
    - if we can drop a card there
  - Instead of drawing all 52 cards, draw each section of board?
    - stop storing location in card in that case
    - take it as parameter to draw function

3/1:

WebGL seems more complicated than it's worth for this project
  - Texture size limits -- one large sprite sheet can't get high enough resolution for fine detail
  - Lots of overhead to set up and draw
  - Hard to believe it would be that much faster than drawImage() for my use case

Got the SVG looking decent, I think
  - Should probably make a head-to-head comparison to answer questions about visual fidelity and performance
  - Make it so I can toggle between them
    - Ideally, I would support doing either way anyway
  - I like the simplicity of drawing things directly on canvas
    - and not having to worry about rasterizing at a given resolution
  - But being able to use SVGs other people have made would be way more flexible
    - The face cards especially -- I'm not going to try to make something like this myself
  - Could try making something that converts the SVG into canvas paths
    - maybe should, just for kicks
    - But how complicate is SVG? Seems like at that point, you should just use built in SVG renderer
    - This way would avoid worrying about resolution, at least

Directly drawing via canvas is still my favorite
  - just in terms of feel and fun. The emjoi cards are kind of cute, and it feels clever to do this
  - But it is pretty limiting, and harder to control precisely
    - different fonts on different systems, especially the emjois

SVG notes
  - The main benefit of the SVG I was using was for the face cards
    - The face cards have lots of fine detail, like hair and clothing designs
    - However, lots of this detail is lost or backfires when cards are smaller
      - Aliasing effects with the lines close together for hair, for instance
    - Not an issue with SVG in general, per se
      - but a note that the fine details I was trying to use from other people's SVGs may not be worth it
  - Also having issues with sizes and rounding

DONE: Trying to make the rendered SVG match my desired card width/height
  - My sizes are slightly off because I'm scaling based on the SVG "width"/"height"
    - Instead of properly dictating size from my desired width/height
  - Got this working the way I want. Looks pretty good, actually.

The SVG seems to look fine when done well.
There isn't a noticeable performance difference on my main machine...
  - but there's a very noticable difference on my old laptop
  - SVG is pretty much instant, text/emoji stutters

2/25/2026:

Taking a break from the roads and intersections madness, want to play with some cards
  - I haven't made a card game before
  - Could try solitaire, or even something simple like Go Fish
  - I feel like the rabbit hole here will probably be the cards themselves
  - Feels like something I could generate
    - Text and emoticons?
  - Could also try finding existing SVG and use those
    - I just like the idea of vector, don't really want to do raster images
      - Although I'm sure there are tons
    - Would it make sense to create my own SVG programatically and draw that?
      - As opposed to just functions to draw?
      - If I have an SVG, is it easier to do transforms with drawImage?
        - Fake 3D rotation by scaling along the Y axis?

Also want to use this opportunity to practice more intentional experimenting with branches
  - Branch for drawing sub images from large SVGs
    - Might screw this file up, guess we'll see

Easy to find SVGs with entire deck of cards in one image
  - Sub-draw SVGs?
    - Does this make sense?

SVG drawing was kind of blurry. Dealing with HiDPI adds more headaches.
  - Got something that worked, but I feel like it's blurry
  - My custom canvas was causing problems.
    - Not sure where, but might be good practice to make a simpler one
    - Generate a matrix for bounds, apply this matrix to mouse coords?
      - Is this any easier than what I'm currently doing saving transform and scale separately?

Made another branch to experiment with text and emojis
  - The numbered cards work nicely
  - The face cards will probably look weird, but I can generate a few just to finish this off
  - The text can be stretched with scaling, so I can do my fake 3D turns

WebGL with rasterized SVG as texture?
  - Wondering how this would look compared to canvas drawing
  - Would make effects like flipping the cards more straightforward (and probably look better)
  - Good practice, if nothing else
